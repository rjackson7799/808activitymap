export default function Home() {
  // Placeholder root. The locale-aware public tree ([locale] + middleware
  // rewrite, EN at root per D3) is built in the public-pages checkpoint.
  return <main>OK</main>;
}
